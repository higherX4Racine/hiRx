# Search a folder for R scripts and list every namespace used in those files

Search a folder for R scripts and list every namespace used in those
files

## Usage

``` r
detect_namespaces(.directory)
```

## Arguments

- .path:

  \`\<chr\>\` the directory to search in

## Value

\`\<chr\>\` a sorted vector of each namespace
