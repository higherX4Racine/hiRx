# Remove all singleton columns

Remove all singleton columns

## Usage

``` r
only_informative_columns(.x)
```

## Arguments

- .x:

  a tibble or data table with more than one row

## Value

a tibble that may have fewer columns
