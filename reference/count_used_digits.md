# Count the number of digits that FIPS uses to track a type of area

Count the number of digits that FIPS uses to track a type of area

## Usage

``` r
count_used_digits(.areas)
```

## Arguments

- .areas:

  The table of area information

## Value

a table of digit counts for each type of area
