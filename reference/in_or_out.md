# Pivot blocks to

Pivot blocks to

## Usage

``` r
in_or_out(blocks, containing_geo, group_id)
```

## Arguments

- blocks:

  the many census blocks

- containing_geo:

  the single covering geo

- group_id:

  the column that defines group membership

## Value

a wider table
