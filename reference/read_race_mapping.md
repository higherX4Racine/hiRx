# Read a csv file that contains mappings between racial labels

Read a csv file that contains mappings between racial labels

## Usage

``` r
read_race_mapping(.file_name)
```

## Arguments

- .file_name:

  the full path to the file

## Value

a tibble of character columns
