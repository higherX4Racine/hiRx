# Define a subfolder of the runtime environment's DATA_PATH

Define a subfolder of the runtime environment's DATA_PATH

## Usage

``` r
input_path(...)
```

## Arguments

- ...:

  path components

## Value

a path to a file or directory
